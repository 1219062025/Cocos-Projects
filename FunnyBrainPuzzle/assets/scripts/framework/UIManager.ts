import InstanceBase from './common/instanceBase';

export class UIManager extends InstanceBase {
  // private uiMap: Map<any, >

  constructor() {
    super();
  }
}

export default UIManager.instance();
